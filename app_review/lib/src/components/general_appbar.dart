import 'package:flutter/material.dart';

  var generateAppBar = AppBar(
      title: const Text('BQ Health', style: styleTitle,),
      centerTitle: true,
    );

const styleTitle = TextStyle(
  fontSize: 14,
  color: Colors.blue,
  );