import 'package:flutter/material.dart';

class TitleIMC extends StatelessWidget {
  const TitleIMC({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('BQ Health', style: TextStyle(fontSize: 20.0));
  }
}
